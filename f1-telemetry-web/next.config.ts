import type { NextConfig } from "next";

// Se usa la configuración por defecto: las credenciales de S3 se leen del
// entorno en las rutas de API y no hace falta exponerlas al cliente.
const nextConfig: NextConfig = {};

export default nextConfig;
